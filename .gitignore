# 依赖相关
node_modules/
uni_modules/

# 编译生成的文件
unpackage/
dist/
build/

# 日志文件
*.log

# 临时文件、缓存文件等
*.tmp
*.swp
.DS_Store
Thumbs.db

# 编辑器/IDE 相关配置（不同编辑器可按需调整补充）
.vscode/
.idea/
*.iml
*.suo
*.user

# 锁文件（根据包管理工具，保留对应需要的，这里都先列出来，实际可按需调整）
package-lock.json
yarn.lock
pnpm-lock.yaml

# UniApp 特定可能的临时或自动生成文件（部分可根据实际情况判断是否保留）
uni.promisify.adaptor.js

# 若有自定义的编译输出目录等，也可添加进来，比如下面假设的自定义输出目录
# custom-build/

# 测试相关（若有单独的测试报告、覆盖率文件等目录，可补充）
coverage/
test-results/